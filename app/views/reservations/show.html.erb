Reservation Show Page

Congrats, your reservation is successful

<!-- <%#= render "/listings/display" %> -->

<table>
  <tr>

    <th>Total Amount</th>
    <th>Guests</th>
    <th>Check-in Date</th>
    <th>Check-out Date</th>
    
    
    <th></th>
  </tr>
 

  <tr>
    <td><%= @reservation.total_price %></td>
    <td></td>
    <td><%= @reservation.guests %></td>
    <td></td>
    <td><%= @reservation.check_in %></td>
    <td></td>
    <td><%= @reservation.check_out %></td>
    
   
  </tr>

</table>

<%= link_to "Edit", edit_listing_reservation_path(@reservation) %>

<%= link_to 'Delete', listing_reservation_path(@reservation),
            :confirm => 'Are you sure?', :method => :delete %>
